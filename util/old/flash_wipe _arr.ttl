; ===== ===== ===== ===== =====
; Subroutine to wipe files
; Asumed globals:
;	delnum
;	files
; Used in models:
;	ISR4331
;	C2960 / C2960X
; ===== ===== ===== ===== =====

if delnum < 1 then 
	newstat = 'No files to delete!'
	include stat
	goto finish
endif

for i 0 49
	ifdefined files[i]
	if result != 1 break
	
	delstr = 'del /r /f flash:'
	strconcat delstr files[i]
	if debug then
		newline = delstr
		include stat
	endif
next